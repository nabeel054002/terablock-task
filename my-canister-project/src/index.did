service: () -> {
    addApproval: (vec nat8) -> ();
    executeApproval: () -> (variant {to:text; value:nat64; from:text}) query;
    readApprovals: (nat32) -> (vec variant {to:text; value:nat64; from:text}) query;
}
